# metadata

The front-end created a concept of `metadata` and has 2 events associated with it:

- `metadata-get`
- `metadata-set`

This package models that, so that it is no longer just stored in a Golang `map[string]any`
